# Workflows

A collection of reusable GitHub Actions workflows for projects.

## Actions

- [`setup-environment/action.yml`](./setup-environment/action.yml): Setup environment and install dependencies
